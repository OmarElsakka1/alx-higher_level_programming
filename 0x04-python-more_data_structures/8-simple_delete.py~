#!/usr/bin/python3
def simple_delete(a_dictionary, key=""):
    new_dict = {}
    for ele in a_dictionary:
        new_dict[ele] = a_dictionary[ele]
    if key in new_dict:
        del new_dict[key]
    return new_dict
